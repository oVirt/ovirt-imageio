[daemon]
poll_interval = 0.1

[tls]
key_file = test/pki/key.pem
cert_file = test/pki/cert.pem
ca_file = test/pki/cert.pem

[remote]
port = 0
host = 127.0.0.1

[local]
socket =

[control]
socket = test/daemon.sock